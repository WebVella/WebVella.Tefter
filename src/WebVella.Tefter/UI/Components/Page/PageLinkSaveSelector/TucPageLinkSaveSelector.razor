@namespace WebVella.Tefter.UI.Components
@inherits TfBaseComponent
@if (_open && ActiveSavedUrl is not null)
{
	<div class="view-export-selector-menu">
		<FluentMenu Width="200" @bind-Open="_open" Anchor="view-export-selector" UseMenuService="false">
			@if (Navigator.IsSpaceViewSavedUrlChanged(ActiveSavedUrl.Url))
			{
				<FluentMenuItem OnClick="_updateUrl">
					Save URL change
					<FluentIcon Value="@TfConstants.GetIcon("Save")" Slot="start" Color="@Color.Accent" />
				</FluentMenuItem>

			}
			<FluentMenuItem OnClick="_editUrl">
				Edit Saved URL
				<FluentIcon Value="@TfConstants.GetIcon("LinkEdit")" Slot="start" Color="@Color.Accent" />
			</FluentMenuItem>
			<FluentMenuItem OnClick="_saveUrlAs">
				Save URL as
				<FluentIcon Value="@TfConstants.GetIcon("LinkMultiple")" Slot="start" Color="@Color.Accent" />
			</FluentMenuItem>
			<FluentMenuItem OnClick="_removeUrl">
				Remove URL save
				<FluentIcon Value="@TfConstants.GetIcon("LinkDismiss")" Slot="start" Color="@Color.Error" />
			</FluentMenuItem>
		</FluentMenu>
	</div>

}

