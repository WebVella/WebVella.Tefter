@inherits LayoutComponentBase
@if (_isLoaded)
{
	<CascadingValue TValue="TfAuthLayout" Name="TfAuthLayout" Value="this" IsFixed="true">
		@* <FluentDesignTheme Mode="@_themeMode" CustomColor="@_accentColor.GetColor().HEX" *@
		@*                    NeutralBaseColor="@TfColor.Zinc500.GetColor().HEX"/> *@
		<FluentDesignTheme Mode="@_themeMode" CustomColor="@_accentColor.GetColor().HEX"/>		
		@Body
		<div id="tf-fluent-providers">
			<FluentToastProvider MaxToastCount="5" Position="ToastPosition.TopCenter"
			                     RemoveToastsOnNavigation="false" Timeout="2000"/>
			<FluentMenuProvider/>
			<FluentDialogProvider/>
			<FluentTooltipProvider/>
			<FluentKeyCodeProvider/>
		</div>
		<div id="global-state-region" style="display: none">
			<TucScreenRegionComponent Region="@(new TfGlobalStateScreenRegion())"/>
		</div>
	</CascadingValue>
}


