@namespace WebVella.Tefter.Web.Components
@inherits TfBaseComponent
@if (SidebarExpanded.Value)
{
	<TfLayoutBodyAside>
		<Toolbar>
			<div class="tf-sidetoolbar">
				<div class="tf-sidetoolbar__search">
					<FluentTextField Class="search" Placeholder="@LOC("search...")" Immediate="true" ImmediateDelay="700" Value=@search
									 ValueChanged="onSearch" AutoComplete="off" />
				</div>
				<div class="tf-sidetoolbar__actions">
					<TfAdminUsersActions />
				</div>
			</div>
		</Toolbar>
		<ChildContent>
			<FluentNavMenu Class="tf-sidenav">
				@foreach (var item in _getRoles())
				{
					<FluentNavLink Href="@(string.Format(TfConstants.AdminRoleDetailsPageUrl, item.Id))" Match="@NavLinkMatch.Prefix"
								   @key="@item.Id" Icon="@TfConstants.AdminUsersIcon">
						@TfConverters.StringOverflow(item.Name, _stringLimit)
					</FluentNavLink>
				}
			</FluentNavMenu>
		</ChildContent>
	</TfLayoutBodyAside>
}
