@namespace WebVella.Tefter.Web.Components
@inherits TfBaseComponent


@if (ItemId == Guid.Empty)
{
	<TfLayoutBodyMain>
		<Header>
			<TfPageHeader>@LOC("Addon Pages")</TfPageHeader>
		</Header>
		<ChildContent>
			<FluentMessageBar Title="No page selected"
							  Style="@("border: 1px solid var(--neutral-base-color)")"
							  Intent="MessageIntent.Info"
							  AllowDismiss="false">
				Select a page from the menu
			</FluentMessageBar>
		</ChildContent>
	</TfLayoutBodyMain>
}
else
{
	<TfScreenRegionComponent RegionContext="@(new TfAdminPageScreenRegionContext())" Scope="@(new TfScreenRegionScope(null,ItemId))" />
}

